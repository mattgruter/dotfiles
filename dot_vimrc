filetype plugin indent on
syntax on
set encoding=utf-8

" spaces instead of tabs
set tabstop=4
set shiftwidth=4
set softtabstop=0
set expandtab

" ... but Makefiles need tabs
autocmd FileType make setlocal noexpandtab

" <jk> = <ESC>
inoremap jk <ESC>

" better leaderkey
let mapleader = "\<Space>"
